<%# TODO: Create a route to get the value from the answers  %>
<div class="container">
  <%= form_with url: "/courses/#{@course.id}/classrooms/#{@classroom.id}/quizz" do |form| %>
    <% Answer.all.each do |answer| %>
    <h3><%= answer.question.content %></h3>
    <% answer.content.split(',').each_with_index do |word, index| %>
      <%= form.check_box :"user_answers[#{answer.id}][#{index}]"%>
      <%= form.label :"user_answers[#{index}]", word %>
      <% end %>
    <% end %>
    <%= form.submit "Validate your answers" %>
  <% end %>
</div>
<%# TODO: Creatde a method to compare the values  %>
<%# TODO: Put a condition on the sbt generation based on the result of the quizz  %>
