<%= simple_form_for [@course, @chapter, @lesson] do |f| %>
  <%= f.input :title %>
  <%= f.input :media, collection: [ "Podcast", "Vidéo", "Text"]  %>
  <%= f.rich_text_area :content %>
  <%= f.button :submit %>
<% end %>
