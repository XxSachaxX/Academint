<%= simple_form_for @course do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :level, collection: [ "Débutant", "Intermédiaire", "Expert"] %>
  <%= f.button :submit %>
<% end %>
